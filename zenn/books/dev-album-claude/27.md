---
title: "STEP 27: tmuxp による環境のコード化"
---

# 
            tmuxp による環境のコード化
          

          
            tmuxp によるセッション構成のコード化と、開発環境の宣言的な管理。
          

        


        
          
          
            ## 
              
              宣言的な環境管理
            
            
              **tmuxp** を使用することで、画面分割や起動コマンドをYAMLファイルとして定義し、一貫した開発環境を瞬時に再現できます。
            


            
              
                
                .tmuxp.yaml

              

              ```yaml
session_name: ai-dev-env
windows:
  - window_name: main
    layout: main-vertical
    options:
      main-pane-width: 60%
    panes:
      - shell_command:
          - cd ~/projects/my-app && claude
      - shell_command:
          - cd ~/projects/my-app && npm run dev
      - shell_command:
          - cd ~/projects/my-app && git log --oneline -10
```
              
                `tmuxp load .` を実行するだけで、定義に基づいたセッションが立ち上がります。
              

            

          


          
          
            ## 
              
              MCPサーバとの組み合わせ
            
            
              MCPサーバは `claude mcp add` で事前登録しておけば、tmuxp のYAML内では単に `claude` を起動するだけで自動接続されます。
            

            ```bash
# MCPサーバの事前登録（プロジェクトスコープ）
$ claude mcp add -s project github-server \
    -- npx -y @modelcontextprotocol/server-github
$ claude mcp add -s project postgres \
    -- npx -y @mcp/server-postgres

# 以降は tmuxp load . で環境起動時に自動接続
```
            :::message
**プロジェクトスコープの活用**
-s project で登録すると .mcp.json に保存され、Git管理でチーム共有可能です。tmuxp.yaml と合わせて、開発環境全体をコード化できます。
:::
          


          
          
            ## 
              自動化のメリット
            
            
              tmux で「作業空間」を固定し、tmuxp で「環境」を構成管理し、Skills と Hooks で「AI」の動作を規定する。
              この組み合わせにより、エンジニアは環境構築やボイラープレートの生成から解放され、より本質的な設計判断に集中できます。
            

          

          ### チャレンジ: tmuxp の YAML 設定を書こう

tmuxp で AI 開発環境を定義する .tmuxp.yaml を書いてください。Claude Code、開発サーバー、テスト実行の3ペインレイアウトを定義しましょう。

```tsx
# .tmuxp.yaml
session_name: my-project
windows:
  - window_name: main
    layout: main-vertical
    panes:
      # ここにペイン設定を書いてください
```

:::details 模範解答
```tsx
# .tmuxp.yaml
session_name: my-project
windows:
  - window_name: main
    layout: main-vertical
    options:
      main-pane-width: 60%
    panes:
      - shell_command:
          - cd ~/projects/my-app && claude
      - shell_command:
          - cd ~/projects/my-app && npm run dev
      - shell_command:
          - cd ~/projects/my-app && npm test -- --watch
```
:::